# Claims Approval System — environment configuration
# Copy this file to .env and fill in your values.
# .env is gitignored and must never be committed.

DB_HOST=localhost
DB_NAME=doc-app
DB_USER=app_user
DB_PASS=change_me
